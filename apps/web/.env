# Build-time configuration for the web app.
# These values are inlined at build time by Next.js.
# Override in .env.local (not committed) or via environment variables.

NEXT_PUBLIC_APP_TITLE="⍺lphaβeta"
