{
  "name": "@alphabeta/analysis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-free stats core shared by the /lab tools and in-bet instrument panels.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "sync:landing": "npm run build && cp dist/power.js ../../apps/landing/lab/sample-size/analysis.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  }
}
